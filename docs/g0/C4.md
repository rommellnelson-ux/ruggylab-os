# G0 — C4 et architecture as-built

> **Chaque élément porte son statut réel.** Une architecture souhaitée
> présentée comme installée est pire qu'une absence de diagramme : elle donne
> confiance à tort.

| Statut | Signification |
| --- | --- |
| `AS_BUILT` | existe et fonctionne, vérifié en CI |
| `OPTIONAL` | existe, hors du cœur distribué, activé au choix de l'exploitant |
| `DISABLED_BY_DEFAULT` | le code existe, l'exécution est coupée par un réglage |
| `PLANNED` | n'existe pas — projeté |

Les services du diagramme Conteneurs sont **vérifiés contre
`docker-compose.yml`** par `tests/test_g0_inventory.py` : un service présent
dans l'un et absent de l'autre fait échouer le test.

## 1. C4 — Contexte système

```mermaid
graph TB
    subgraph "CSA GR Plateau"
        BIO["Biologiste / Technicien<br/>AS_BUILT"]
        OFF["Officier / Médecin<br/>AS_BUILT"]
        COMPTA["Comptable<br/>AS_BUILT"]
        ADMIN["Administrateur<br/>AS_BUILT"]
    end

    RL["<b>RUGGYLAB OS</b><br/>Système de gestion de laboratoire<br/>AS_BUILT — REAL_DATA_NO_GO"]

    BIO --> RL
    OFF --> RL
    COMPTA --> RL
    ADMIN --> RL

    CSA["Plateforme CSA / Supabase<br/>DISABLED_BY_DEFAULT<br/>CSA_SYNC_ENABLED=false"]
    ONMCI["ONMCI<br/>DISABLED_BY_DEFAULT<br/>fail-closed"]
    AUTO["Automates DH36 / POCT<br/>DISABLED_BY_DEFAULT<br/>aucun automate qualifié"]
    CDN["CDN Leaflet / JsBarcode<br/>AS_BUILT — depuis le NAVIGATEUR"]
    OSM["Tuiles OpenStreetMap<br/>AS_BUILT — depuis le NAVIGATEUR"]
    DHIS["DHIS2<br/>PLANNED"]

    RL -.->|désactivé| CSA
    RL -.->|désactivé| ONMCI
    AUTO -.->|désactivé| RL
    BIO -->|charge directement| CDN
    BIO -->|charge directement| OSM
    RL -.->|non implémenté| DHIS

    classDef asbuilt fill:#1b5e20,stroke:#0d3d10,color:#fff
    classDef disabled fill:#5d4037,stroke:#3e2723,color:#fff
    classDef planned fill:#37474f,stroke:#1c262b,color:#fff,stroke-dasharray: 5 5
    class RL,BIO,OFF,COMPTA,ADMIN,CDN,OSM asbuilt
    class CSA,ONMCI,AUTO disabled
    class DHIS planned
```

> **Les CDN partent du navigateur, pas du serveur.** RUGGYLAB ne les appelle
> jamais ; c'est le poste client qui les joint. La distinction compte pour la
> conformité comme pour un déploiement hors ligne.

## 2. C4 — Conteneurs

```mermaid
graph TB
    NAV["Navigateur<br/>AS_BUILT"]

    subgraph CORE["RUGGYLAB Core — docker-compose.yml"]
        PROXY["<b>proxy</b> · Caddy 2.8<br/>TLS, seul à publier 80/443<br/>AS_BUILT"]
        APP["<b>app</b> · uvicorn<br/>API, UI, WebSocket<br/>AS_BUILT"]
        SCHED["<b>scheduler</b><br/>tâches périodiques<br/>AS_BUILT"]
        GW["<b>analyzer-gateway</b><br/>hôte des interfaces<br/>AS_BUILT / interfaces DISABLED_BY_DEFAULT"]
        PG[("<b>postgres</b> 16.6<br/>49 tables<br/>AS_BUILT")]
        VK[("<b>valkey</b> 8.1.9<br/>cache, files, verrous<br/>AS_BUILT")]
        PROM["<b>prometheus</b> v3.1.0<br/>collecte /metrics<br/>AS_BUILT"]
        BKP["<b>db-backup</b><br/>dump vérifié SHA-256<br/>AS_BUILT"]
        MIG["<b>migrate</b><br/>profil manuel<br/>AS_BUILT"]
    end

    GRAF["<b>grafana</b> 11.0.0 · AGPL-3.0<br/>docker-compose.monitoring.yml<br/>OPTIONAL — hors du cœur"]

    NAV -->|HTTPS 443| PROXY
    PROXY -->|HTTP interne| APP
    APP --> PG
    APP --> VK
    SCHED --> PG
    SCHED --> VK
    GW --> PG
    GW --> VK
    PROM -->|scrape /metrics| APP
    BKP --> PG
    MIG --> PG
    GRAF -.->|optionnel| PROM

    classDef asbuilt fill:#1b5e20,stroke:#0d3d10,color:#fff
    classDef optional fill:#4a148c,stroke:#2a0845,color:#fff,stroke-dasharray: 5 5
    classDef disabled fill:#5d4037,stroke:#3e2723,color:#fff
    class PROXY,APP,SCHED,PG,VK,PROM,BKP,MIG,NAV asbuilt
    class GW disabled
    class GRAF optional
```

**Neuf services dans le cœur.** Grafana n'y est pas : il vit dans un overlay
séparé, et l'absence de Grafana est le **mode nominal supporté**, pas un mode
dégradé.

## 3. C4 — Composants du conteneur `app`

```mermaid
graph TB
    subgraph APP["app — 230 modules, 32 406 lignes"]
        API["<b>api/v1/endpoints</b><br/>53 modules · 231 opérations<br/>AS_BUILT"]
        WS["<b>WebSocket</b> /api/v1/notifications/ws<br/>fan-out temps réel<br/>AS_BUILT"]
        SRV["<b>services</b><br/>logique métier<br/>AS_BUILT"]
        MOD["<b>models</b><br/>SQLAlchemy · 49 tables<br/>AS_BUILT"]
        CORE2["<b>core</b><br/>config, sécurité, cache<br/>AS_BUILT"]
        TPL["<b>templates + static</b><br/>cockpit HTML/JS<br/>AS_BUILT"]
        ML["<b>inférence paludisme</b><br/>modèle ONNX absent du dépôt<br/>DISABLED_BY_DEFAULT"]
    end

    API --> SRV
    WS --> SRV
    SRV --> MOD
    SRV --> CORE2
    API --> CORE2
    TPL -.->|appels authentifiés| API
    SRV -.->|inactif| ML

    classDef asbuilt fill:#1b5e20,stroke:#0d3d10,color:#fff
    classDef disabled fill:#5d4037,stroke:#3e2723,color:#fff
    class API,WS,SRV,MOD,CORE2,TPL asbuilt
    class ML disabled
```

## 4. Frontières de confiance et de réseau

```mermaid
graph LR
    subgraph INTERNET["Internet — NON FIABLE"]
        EXT["CDN · tuiles OSM · registres"]
    end
    subgraph LAN["Réseau local CSA — SEMI-FIABLE"]
        POSTE["Postes utilisateurs"]
    end
    subgraph FRONT["frontend_net"]
        P["proxy"]
    end
    subgraph BACK["backend_net — NON PUBLIÉ"]
        A["app · scheduler · gateway"]
        D[("postgres · valkey")]
    end
    subgraph MGMT["management_net — VPN/bastion"]
        M["prometheus"]
    end
    subgraph ANA["analyzer_net — DÉSACTIVÉ"]
        AN["listeners automates"]
    end

    POSTE -->|"HTTPS 443 — SEUL flux entrant"| P
    P -->|HTTP interne| A
    A --> D
    M -->|scrape| A
    POSTE -.->|"le NAVIGATEUR joint le CDN,<br/>pas le serveur"| EXT
    AN -.->|coupé| A

    classDef untrusted fill:#b71c1c,stroke:#7f0000,color:#fff
    classDef semi fill:#e65100,stroke:#a03c00,color:#fff
    classDef trusted fill:#1b5e20,stroke:#0d3d10,color:#fff
    classDef off fill:#424242,stroke:#212121,color:#fff,stroke-dasharray: 5 5
    class EXT untrusted
    class POSTE semi
    class P,A,D,M trusted
    class AN off
```

### Flux autorisés et interdits

| Flux | État | Vérifié par |
| --- | --- | --- |
| Poste → proxy, HTTPS 443 | **autorisé** — seul flux entrant | `docker-stack` : seul le proxy publie des ports |
| Proxy → `/metrics`, `/docs`, `/redoc`, `/openapi.json` | **interdit** — 404 | `docker-stack` §14 |
| Prometheus → `app:8000/metrics` | autorisé, réseau interne | `docker-stack` |
| App → CSA / ONMCI | **coupé par défaut** | `CSA_SYNC_ENABLED=false` |
| Automate → gateway | **coupé par défaut** | `ENABLE_DH36_LISTENER=false` |
| Bind analyseur sur `0.0.0.0` | **refusé au démarrage** | `app/core/config.py` |
| Navigateur → CDN, tuiles | autorisé, **hors serveur** | — |

## 5. Points uniques de panne

| Composant | Effet de sa perte | Continuité |
| --- | --- | --- |
| **`postgres`** | arrêt total | sauvegarde vérifiée + restauration testée en CI |
| **`proxy`** | plus d'accès utilisateur | interne intact ; redémarrage du conteneur |
| `app` | plus d'API | conteneur unique — `restart: unless-stopped` |
| `valkey` | dégradation | cache reconstitué ; **denylist de jetons perdue** |
| `scheduler` | tâches suspendues | pas de perte de données |
| `prometheus` | plus de métriques | aucun effet clinique |
| `grafana` | **aucun** | hors du cœur |

> **Un seul exemplaire de chaque service.** Aucune redondance, aucun
> basculement automatique : c'est une architecture mono-hôte, adaptée à un
> centre unique. La continuité repose sur la **sauvegarde** et sur le
> **fonctionnement papier de secours**, pas sur la haute disponibilité. Le
> dire est plus utile que de laisser croire l'inverse.

## 6. Mécanismes de continuité

| Mécanisme | Statut |
| --- | --- |
| Sauvegarde PostgreSQL automatique, somme SHA-256 | `AS_BUILT` |
| Restauration dans une base vierge, testée en CI | `AS_BUILT` |
| Copie hors machine | **`PLANNED`** — non instrumentée |
| Redémarrage automatique des conteneurs | `AS_BUILT` |
| Verrou de migration au démarrage | `AS_BUILT` |
| Mode dégradé papier | `AS_BUILT` (procédure), hors logiciel |
| Réplication / bascule | **`PLANNED`** — inexistant |

> **La copie hors machine est `PLANNED`, et c'est un P1.** Une sauvegarde qui
> reste sur la machine sauvegardée ne protège pas d'une perte de la machine.
> Transmis au lot D.
